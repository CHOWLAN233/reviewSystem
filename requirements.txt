# =============================================================================
# Review Agent – Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt

# File parsing
python-pptx>=0.6.23         # PPTX text & notes extraction
PyMuPDF>=1.24.0             # PDF text extraction
olefile>=0.47               # Legacy .ppt OLE extraction

# LLM abstraction – unified interface to 100+ model providers
litellm>=1.40.0

# Configuration
python-dotenv>=1.0.0        # .env file loading

# Web UI
streamlit>=1.35.0           # Interactive web interface

# MD → PDF conversion
markdown>=3.5               # Markdown to HTML
playwright>=1.40.0          # Headless Chromium for HTML → PDF

# =============================================================================
# Optional / Development
# =============================================================================
# pytest>=8.0.0             # Testing framework
# pytest-mock>=3.12.0       # Mocking support
